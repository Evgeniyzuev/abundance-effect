# AI Enhancement Plan: База Знаний и Инструменты Контекста

## 📋 Текущее состояние
- ✅ Базовый чат с Gemini/Groq
- ✅ Минимальный контекст: баланс, уровень, желания
- ⚠️ Ограниченное понимание системы Abundance Effect
- ⚠️ Нет доступа к динамическим данным БД
- ⚠️ Нет проактивной генерации заданий

---

## 🎯 Цель развития AI

> **AI должен стать умным координатором, который:**
> 1. Понимает всю экосистему Abundance Effect
> 2. Запрашивает нужный контекст из БД по требованию
> 3. Создает персональные челленджи для роста пользователей
> 4. Заменяет бесполезные игры и соцсети увлекательной системой роста

---

## 🛠️ Решение 1: Инструменты для запроса контекста (AI Tools/Functions)

### Концепция
Использовать **Function Calling** (доступно в Gemini и Groq) - AI сам решает, когда нужны данные из БД.

### Инструменты для AI:

#### 1. `getUserChallenges(userId)` 
**Описание**: Получить список активных/завершенных челленджей пользователя
```typescript
// AI видит описание:
{
  name: "getUserChallenges",
  description: "Get user's challenges (active, completed, failed)",
  parameters: {
    status: "all | active | completed"
  }
}
```

#### 2. `getUserProjects(userId)`
**Описание**: Получить проекты пользователя (активные заявки, завершенные)

#### 3. `getTeamActivity(userId)`
**Описание**: Получить активность команды (лида и рефералов)
- Кто из команды выполнил последние челленджи
- Рост Core команды за неделю

#### 4. `getSystemFAQ(query)`
**Описание**: База знаний системы - AI может искать ответы на вопросы пользователей
- Примеры: "Как работает Core?", "Как вывести деньги?", "Что дает уровень?"

#### 5. `getRecommendedChallenges(userId)`
**Описание**: Получить рекомендуемые челленджи на основе:
- Текущего уровня
- Завершенных челленджей
- Навыков и интересов

#### 6. `createCustomChallenge(title, description, reward)`
**Описание**: AI создает персональное задание для пользователя
- Привязано к его желаниям из Wishboard
- Адаптировано под уровень

#### 7. `getMarketplaceOffers(category)`
**Описание**: Услуги и товары других пользователей (будущая функция)

#### 8. `getGuidance(topic)`
**Описание**: Руководства по темам:
- `onboarding` - как начать
- `core_growth` - как быстрее расти
- `referrals` - как привлекать команду
- `wishes` - как правильно ставить цели

---

## 🗂️ Решение 2: База Знаний (Knowledge Base)

### Таблица БД: `ai_knowledge_base`

```sql
CREATE TABLE ai_knowledge_base (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  category TEXT NOT NULL, -- 'faq', 'guide', 'concept', 'feature'
  question TEXT, -- для FAQ
  keywords TEXT[], -- для поиска
  content TEXT NOT NULL, -- ответ или описание
  language TEXT DEFAULT 'en',
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Примеры записей:

#### FAQ
```json
{
  "category": "faq",
  "question": "Как работает Abundance Core?",
  "keywords": ["core", "ядро", "проценты", "доход"],
  "content": "Abundance Core - это ваш несгораемый капитал. Он генерирует 0.000633% в день (26% годовых) пожизненно. Чем больше Core, тем выше ваш уровень и ежедневный доход.",
  "language": "ru"
}
```

#### Руководства
```json
{
  "category": "guide",
  "question": "Как быстро вырасти с уровня 1 до уровня 10?",
  "keywords": ["рост", "уровень", "прогресс"],
  "content": "Путь до уровня 10 ($1,000 Core):\n1. Выполняйте ежедневные челленджи (+$5-20)\n2. Пополняйте Core из Wallet\n3. Привлекайте рефералов (бонус 1% от их роста)\n4. Участвуйте в проектах\n5. Реинвестируйте проценты",
  "language": "ru"
}
```

#### Концепции системы
```json
{
  "category": "concept",
  "question": "Что такое Abundance Effect?",
  "keywords": ["abundance", "система", "изобилие", "purpose"],
  "content": "Abundance Effect - это экосистема, где AI координирует действия пользователей для исполнения их желаний. Вместо бесцельного скроллинга соцсетей, вы получаете:\n- Финансовый рост (Core 26% годовых)\n- Реальные навыки через челленджи\n- Поддержку сообщества\n- Путь к исполнению мечты",
  "language": "ru"
}
```

### Функция поиска в БД:
```typescript
async function searchKnowledgeBase(query: string, language: string) {
  // Поиск по keywords или full-text search
  const results = await supabase
    .from('ai_knowledge_base')
    .select('*')
    .or(`keywords.cs.{${query}},content.ilike.%${query}%`)
    .eq('language', language)
    .limit(3);
  
  return results.data;
}
```

---

## 🧠 Решение 3: Расширенный System Prompt

### Добавить в контекст AI:

```typescript
function buildEnhancedSystemContext(userContext: any) {
  return `
[SYSTEM CONTEXT]
You are the **Abundance Coordinator AI**.

## The Abundance Effect Platform
Abundance Effect is a system that replaces destructive habits (doomscrolling, games) with constructive growth:
- **Core**: User's eternal capital generating 26% APY
- **Levels**: 1-40, each requires ~2x more Core
- **Challenges**: Daily tasks teaching real skills and earning rewards
- **Wishboard**: Users visualize dreams, AI helps materialize them
- **Teams**: Users help each other grow (leaders earn 1% of team's Core growth)
- **Vision**: AI Sims showing future based on user's actions

## Your Mission
1. **Generate Personalized Challenges**: Create tasks aligned with user's wishes
2. **Coordinate Users**: Connect people who can help each other
3. **Teach Growth**: Explain how to level up faster
4. **Keep Engaged**: Make the system more fun than social media
5. **Answer Questions**: Use knowledge base to explain features

## Tools Available
You can request additional data:
- getUserChallenges() - see what user already completed
- getSystemFAQ() - answer system questions
- createCustomChallenge() - generate personalized task
- getTeamActivity() - see team progress

## User State:
${JSON.stringify(userContext, null, 2)}

## Behavior:
- Be proactive: suggest challenges even if not asked
- Be brief: mobile users prefer short answers
- Be motivating: focus on growth, not limits
- Use emojis: 🎯 📈 💰 ✨ (but don't overdo)
[/SYSTEM CONTEXT]
`;
}
```

---

## 🎮 Решение 4: Проактивная генерация челленджей

### Алгоритм создания персональных заданий:

```typescript
async function generatePersonalizedChallenge(userId: string, wish: any) {
  // 1. Анализ желания
  const wishCost = wish.estimated_cost;
  const userLevel = userContext.level;
  
  // 2. Подбор типа задания
  let challengeType = '';
  if (wishCost < 100) challengeType = 'quick_earn';
  else if (wishCost < 1000) challengeType = 'skill_development';
  else challengeType = 'project_based';
  
  // 3. Генерация через AI
  const prompt = `
    User wants: ${wish.title} (cost: $${wishCost})
    Current level: ${userLevel}
    
    Create a realistic challenge that:
    1. Helps earn money towards this wish
    2. Teaches a valuable skill
    3. Takes 1-7 days to complete
    4. Reward: $5-50 depending on difficulty
    
    Format:
    Title: ...
    Description: ...
    Steps: 1) ... 2) ... 3) ...
    Reward: $X Core
    Verification: How to prove completion
  `;
  
  const aiResponse = await chatWithAI(prompt, [], userContext);
  
  // 4. Сохранение в БД как challenge
  await createChallenge(userId, aiResponse);
}
```

### Примеры сгенерированных челленджей:

**Желание: "Купить iPhone 15" ($1000)**
```
🎯 Challenge: "Создай обучающий контент"
📝 Описание: Сними короткое видео (60 сек) о том, как работает Abundance System
🎁 Награда: $20 Core
✅ Проверка: Опубликуй в Instagram/TikTok c #AbundanceEffect, отправь ссылку
```

**Желание: "Отпуск на Бали" ($3000)**
```
🎯 Challenge: "Запусти мини-услугу"
📝 Описание: Предложи услугу в своей сфере (дизайн/копирайт/консультация) команде Abundance
🎁 Награда: $50 Core + возможные заказы
✅ Проверка: Создай карточку в Projects, получи 3+ заявки
```

---

## 📊 Решение 5: Аналитика для AI

### Данные, которые AI должен анализировать:

#### Таблица: `user_analytics`
```sql
CREATE TABLE user_analytics (
  user_id UUID REFERENCES users(id),
  date DATE DEFAULT CURRENT_DATE,
  
  -- Активность
  challenges_completed INT DEFAULT 0,
  time_in_app_minutes INT DEFAULT 0,
  tabs_visited TEXT[], -- ['goals', 'ai', 'wallet']
  
  -- Финансы
  core_growth DECIMAL DEFAULT 0,
  wallet_transactions INT DEFAULT 0,
  
  -- Социальное
  team_interactions INT DEFAULT 0,
  
  PRIMARY KEY (user_id, date)
);
```

### AI использует это для:
- **Персонализации**: "Вижу, вы часто в Goals, но редко в Projects - попробуйте!"
- **Мотивации**: "За последнюю неделю вы выполнили 35 челленджей - отличный темп!"
- **Предупреждения**: "Вы не заходили 3 дня - вот быстрый челлендж на $10"

---

## 🚀 План реализации

### Фаза 1: Инструменты (1-2 дня)
- [ ] Реализовать Function Calling в Gemini
- [ ] Создать базовые функции: getUserChallenges, getSystemFAQ
- [ ] Протестировать вызов функций из AI

### Фаза 2: База Знаний (1 день)
- [ ] Создать таблицу ai_knowledge_base
- [ ] Заполнить 20-30 FAQ и гайдов
- [ ] Реализовать поиск по keywords

### Фаза 3: Расширенный промпт (0.5 дня)
- [ ] Обновить buildSystemContext с концепциями системы
- [ ] Добавить примеры поведения AI

### Фаза 4: Генерация челленджей (2 дня)
- [ ] Логика анализа желаний
- [ ] AI-генерация персональных заданий
- [ ] Сохранение в таблицу challenges

### Фаза 5: Аналитика (1 день)
- [ ] Создать user_analytics
- [ ] Внедрить трекинг активности
- [ ] Передавать данные в AI контекст

---

## 💡 Дополнительные идеи

### 1. AI Squad (Разные роли)
Вместо одного AI - несколько специализированных:
- 🏋️ **Mentor** - мотивация и постановка целей
- 💰 **Investor** - финансовые советы, рост Core
- 🎨 **Creator** - помощь с контентом и проектами
- 👥 **Connector** - связывает с нужными людьми из команды

### 2. Voice AI
- Голосовое общение (whisper API)
- Быстрые команды: "Сколько мне до следующего уровня?"

### 3. Эмоциональный интеллект
- Распознавание настроения по сообщениям
- Адаптация тона: поддержка при неудачах, празднование успехов

### 4. Авто-журналирование
- AI ведет дневник пользователя
- Еженедельные инсайты: "На этой неделе вы..."

---

## 🎯 Приоритет (что делать первым)

1. **База Знаний** (самое быстрое, сразу улучшит ответы)
2. **Function Calling** (дает AI доступ к реальным данным)
3. **Расширенный промпт** (AI понимает миссию системы)
4. **Генерация челленджей** (главная ценность!)

---

## ✅ Ожидаемый результат

После реализации AI сможет:
- ✅ Отвечать на любые вопросы о системе (из Knowledge Base)
- ✅ Видеть прогресс пользователя (через функции БД)
- ✅ Создавать персональные задания под желания
- ✅ Мотивировать и вовлекать (лучше соцсетей!)
- ✅ Координировать пользователей для взаимопомощи

**Превращая AI из простого чат-бота в умного наставника и координатора.**
