# Финальная настройка Telegram Authentication

## ✅ Что реализовано:

Теперь на странице `/login` есть **4 кнопки**:

1. **Continue with Google** - OAuth вход через Google
2. **Telegram Login Widget** - Вход через Telegram на сайте (возвращает на сайт)
3. **Open Telegram Mini App** - Открывает бота для входа через Mini App
4. **Continue with Apple** - Отключена (можно удалить)

---

## 🔧 Необходимые шаги:

### Шаг 1: Добавьте TELEGRAM_BOT_TOKEN в `.env.local`

Откройте файл `.env.local` и добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_bot_token_от_BotFather
```

**Где взять токен:**
1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Если бот уже создан, отправьте `/mybots`
3. Выберите вашего бота → API Token
4. Скопируйте токен (например: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

### Шаг 2: Настройте домен в BotFather

Для работы Telegram Login Widget нужно настроить домен:

1. Откройте [@BotFather](https://t.me/botfather)
2. Отправьте `/setdomain`
3. Выберите вашего бота
4. Введите домен:
   - Для разработки: `localhost` (или оставьте пустым)
   - Для production: `abundance-effect.vercel.app` (ваш домен)

---

## 📋 Как работают кнопки:

### 1. Google OAuth ✅
- Стандартный OAuth flow
- Работает везде
- Создает пользователя с `telegram_id = null`

### 2. Telegram Widget (новая!) 🆕
- **Для веб-сайта** (не в Telegram приложении)
- Открывает popup Telegram для авторизации
- Возвращает на ваш сайт
- Создает пользователя с `telegram_id`
- **Требует**: `TELEGRAM_BOT_TOKEN` в `.env.local`

### 3. Telegram Mini App ✅
- **Для Telegram бота**
- Открывает бота в новой вкладке
- Работает только внутри Telegram
- Автоматическая авторизация через `UserContext`

---

## 🧪 Тестирование:

### На localhost:

1. **Google**: Работает ✅
2. **Telegram Widget**: Работает после добавления `TELEGRAM_BOT_TOKEN` ✅
3. **Telegram Mini App**: Откроет бота в браузере (нужно открыть в Telegram)

### В Telegram (после деплоя):

1. Откройте бота
2. Нажмите "Open App"
3. Автоматический вход через Mini App ✅

---

## ⚠️ Важно:

1. **Telegram Widget** требует `TELEGRAM_BOT_TOKEN` для проверки подписи
2. **Не коммитьте** `.env.local` в Git (уже в `.gitignore`)
3. **На Vercel** добавьте `TELEGRAM_BOT_TOKEN` в Environment Variables

---

## 🎯 Следующие шаги:

1. ✅ Добавьте `TELEGRAM_BOT_TOKEN` в `.env.local`
2. ✅ Настройте домен в BotFather (`/setdomain`)
3. ✅ Перезапустите dev server: `npm run dev`
4. ✅ Протестируйте все 3 способа входа
5. ✅ Задеплойте на Vercel
6. ✅ Добавьте `TELEGRAM_BOT_TOKEN` в Vercel Environment Variables

---

## 📝 Структура файлов:

```
app/
├── api/
│   └── auth/
│       ├── telegram-user/route.ts      # Mini App auth
│       └── telegram-widget/route.ts    # Widget auth (новый!)
├── login/page.tsx                      # Страница логина (обновлена!)
└── ...

components/
└── TelegramLoginButton.tsx             # Widget component (не используется)
```

---

## 🔍 Отладка:

Если Telegram Widget не работает:

1. Проверьте консоль браузера на ошибки
2. Убедитесь, что `TELEGRAM_BOT_TOKEN` добавлен
3. Проверьте, что домен настроен в BotFather
4. Проверьте Network tab - запрос к `/api/auth/telegram-widget`

Если возникнет ошибка `Invalid hash` - проверьте правильность `TELEGRAM_BOT_TOKEN`.
